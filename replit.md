# PulseEDU

PulseEDU is a multi-tenant application providing tools for school operations, student support, and insights.

## Run & Operate

- `pnpm run typecheck`: Full typecheck across all packages.
- `pnpm run build`: Typecheck + build all packages.
- `pnpm --filter @workspace/api-spec run codegen`: Regenerate API hooks and Zod schemas from OpenAPI spec.
- `pnpm --filter @workspace/db run push`: Push DB schema changes (dev only).
- `pnpm --filter @workspace/api-server run dev`: Run API server locally.

## Stack

- **Monorepo tool**: pnpm workspaces
- **Node.js version**: 24
- **Package manager**: pnpm
- **TypeScript version**: 5.9
- **API framework**: Express 5
- **Database**: PostgreSQL + Drizzle ORM
- **Validation**: Zod (`zod/v4`), `drizzle-zod`
- **API codegen**: Orval (from OpenAPI spec)
- **Build**: esbuild (CJS bundle)

## Where things live

- `lib/db/src/schema/`: Database schemas (source of truth).
- `artifacts/api-server/src/routes/`: API endpoints.
- `artifacts/client/src/`: Frontend application code.
- `artifacts/client/src/index.css`: Global CSS, including UI style conventions.
- `artifacts/api-server/src/lib/scope.ts`: Multi-tenancy and school-scoping helpers.
- `artifacts/api-server/src/seed.ts`: Database seeding logic.
- `artifacts/api-server/src/lib/coreTeam.ts`: Core team role definitions.
- `artifacts/client/src/parent/`: Standalone parent portal application.
- `artifacts/mockup-sandbox/src/`: Mockup components for design review.

## Architecture decisions

- **Multi-Tenancy**: `school_id` is central to data isolation, filtering every read/write. SuperUser role is district-wide, others are school-scoped. `req.schoolId` in middleware ensures correct context.
- **API Versioning/Schema Evolution**: Drizzle-kit is not used for non-interactive schema changes; direct SQL `ALTER TABLE IF NOT EXISTS` is preferred for additive changes to avoid blocking interactive prompts.
- **Object Storage**: Custom object storage routes (`/api/storage/*`) with ACLs enforce school-level tenant isolation for uploaded assets (e.g., store item thumbnails) using `bindObjectToSchool` and `pendingUploads` ledger.
- **Client-Side Routing/Bundling**: The application uses a single Vite client with path-based dispatch (`/signage/*`, `/parent/*`, `/*`) handled in `main.tsx` to serve different user experiences (signage, parent portal, staff app) from a unified bundle.
- **UI Design Philosophy**: Screenshots from other products are treated as conceptual references for "what" to build, not "how" to build it. PulseEDU designs from scratch, focusing on improved functionality and distinct visual identity.

## Product

Concise feature list. Full implementation detail (most-recent first) lives in `docs/shipped.md`.

- **Display Management**: Per-school digital-signage playlists (image/video/audio/PDF) with scheduling, PBIS house standings, active hall passes, and Heartbeat signage. Includes **Live Remote Control** — drive every TV on a playlist PowerPoint-style (auto / manual / presentation modes) without re-entering URLs.
- **Hall Pass / Kiosk**: Door kiosk (`/kiosk`) for student self-serve passes with destinations, a period-aware waiting queue (cap 5, resets on the default bell schedule), keep-apart + daily-limit blocking, a teacher Companion Queue panel, and printable activation cards + student badges. Demo guide: `docs/hall-pass-demo-runbook.md`. **Bulk destination management**: the per-teacher destination allowlist is keyed by stable `staffId` (display-name fallback only for legacy null rows). **Restroom areas** (`locations.restroom_area`) bundle the boys+girls variant rows — assign the area once and both grants expand; **facilities** (`locations.school_wide_default`) are granted to every teacher and kept out of the upload. The 187-row grid stays as the manual edge-case editor. **CSV round-trip**: download a pre-filled template (Teacher · LOCKED email matcher · Room · Restroom Area), edit in Excel, upload → preview → commit → undo. Matched by **email** (display-name only when unambiguous); only listed teachers change, their non-restroom grants are preserved, unmatched/unknown-area rows are loudly surfaced. Rollback ledger `teacher_allowlist_import_batches` snapshots prior grants as a **staffId-keyed array** (`prior_json`) so duplicate display names each restore correctly. **Zone rules** (`teacher_allowlist_zone_rules`): map an inclusive room-NUMBER range → restroom area (first match wins); rules pre-fill the template's area column (current single area → zone suggestion → blank) and power one-click **Auto-assign all** which reuses the same `computeAndApplyBulk` preview/commit/rollback path. **Invariant: CSV bulk + zone auto-assign share `computeAndApplyBulk`; the rollback snapshot MUST stay staffId-keyed (array), never name-keyed, or same-name teachers clobber each other's undo.**
- **Safety Plans**: Per-student behavioral/physical safety checklists with library items, audit logs, and role-based access (Guidance Counselor/Core Team edit, all staff view).
- **Eligibility Hub**: Attendance/tardy-driven participation eligibility for athletics, clubs, and activities (tenant-scoped via `req.schoolId`). Managers (admin/Core Team/AD/front-office via `canManageEligibility`) manage activities, members, coaches, and uploads; coaches assigned to an activity get a **read-only** view of their own rosters. Eligibility is computed from the latest `eligibility_absences` upload; supports a weekly eligibility digest and per-student parent notes (`eligibilityParentNoteCap`). See `routes/eligibility.ts`.
- **PBIS Hub & Store**: PBIS point tracking, student recognition, and two reward catalogs (Classroom Store + School Store). School Store is school-wide, read-only for teachers, full edit for admins/PBIS coordinators. **Invisible Student alert is TIER-AWARE**: a student is "invisible" (0 non-voided recognitions) within the window for their highest active MTSS tier — Tier 1 (no active plan)/2/3 default to 8/5/3 school days, all school-configurable (`schoolSettings.pbisInvisibleDaysTier1/2/3`; legacy flat `pbisInvisibleStudentDays` retained but unused). **Invariant: the two surfaces that flag invisibility — `/pbis/needs-attention` and the Teacher Roster — must use identical logic so they always agree on who is invisible.**
- **MTSS Intervention Plans**: Behavior + Academic Tier 2/3 plan tracking — goal setting, weekly progress monitoring, strategy categories, completion reports, tier-aware launcher, bell notifications. Academic plans are keyed by `fastSubject` (ela|math): Tier 2 academic is LIGHT (intensive class is the monitoring — no bell/check-ins); Tier 3 academic is closely monitored on configurable `meetingDays` (bell + per-meeting-day check-ins, week incomplete until each scheduled day logged). "Generate suggestions" runs a **Tier 3 Academic** dual-gate engine: a student surfaces only when FAST **PM1 = Level 1** AND iReady **AP1** is below a per-grade per-subject cut (coordinator fills a cut-score grid; `schoolSettings.ireadyAp1Cuts`). One row per student+subject (expandable weak standards) for one-click Tier 3 academic plan creation. (The earlier light-Tier-2 list was removed from this panel.)
- **Parent Portal**: Secure portal for a student's HeartBEAT data (PBIS, hall passes, tardies, accommodations, staff notes) with admin invites, sibling switching, configurable section visibility, and PDF export. **Staff "Print HeartBEAT"**: a button next to "Preview as parent" on the Parent Access page (Admin/Core Team) downloads the SAME family HeartBEAT PDF a parent gets, for data chats — `GET /api/staff/heartbeat.pdf?studentId=<numericDbId>`, staff-authed + visibility-scoped via `getVisibleStudentIds` + school-scoped, no email/parent-account. Reuses `renderSnapshotPdf` + a shared `assembleSnapshot(studentId, ctx)` core split from `buildParentSnapshot` (staff mode: no parent identity, section visibility falls back to school HeartBEAT defaults; `localSisId` only). **Invariant: out-of-scope AND non-existent students both return an indistinguishable 404 so staff can't probe existence; the parent-mode snapshot path is unchanged by the refactor.** **Official absences, omit-when-unused**: the HeartBEAT surfaces show OFFICIAL days-absent (`attendance.official` from `lib/attendanceMetrics.ts` `loadAttendanceMetrics` — latest Eligibility Hub upload, same source as Roster/Insights); the kiosk-derived absence estimate + disclaimer were removed (Lost Instruction total = hall passes + tardies only). Every attendance metric omits when its source system isn't in use: no hub upload → no absence tile (null, never 0), no attendance-day rows → % rows omitted, streak tiles gated on `countedPeriods > 0`, kiosk arrivals on `checkinCount > 0`. **Invariant: no dashes/estimates — the hub's approximate % is never shown; `student_attendance_day` stays the sole attendance-% source.**
- **Parent Notifications**: Admin-only panel (Family → Parent Notifications, `canManageSettings`) to turn each automated/recurring parent email on/off. **All default to current behavior** (no change until flipped). Toggleable: HeartBEAT weekly email, Eligibility, PBIS milestone, Tardy, Store-ready, Family Messages, Event tickets, E-sign, Tour nurture. Portal invites + password resets are access-critical and intentionally NOT listed. **Reuses existing switches** (HeartBEAT `allow_weekly_email`, `featureFamilyComm`, `featureSchoolStoreNotify`, `tourFamilyNurtureEnabled`); five new `school_settings` cols (default TRUE) back the rest. Each toggle is enforced at its server **send site** via `lib/parentNotify.ts` `isParentNotifyEnabled(schoolId, key)` (`?? true` default preserves behavior), never client-only. Eligibility gates ONLY the parent send (coach/principal/digest copies unaffected). **Invariant: every toggle must short-circuit at the send site via `isParentNotifyEnabled` (client gating is bypassable); the helper's `?? true` default is load-bearing.**
- **Insights Dashboards**: Engagement, Behavior, Academics, SEB/SEL, Equity, Early Warning — aggregates, trends, top-N, grade/window filters, demographic disaggregation, and drill-down to student profiles. **Per-student metric columns** (READ-ONLY, additive): **Days Absent + approx %** (from `lib/attendanceMetrics.ts` `loadAttendanceMetrics` → latest `eligibility_absences` upload for the current semester; % is a weekday-count estimate vs configured semester start, school-local tz, null when no semester start), **Pts to Next FAST Sub-Level** (`bucketFor().gap`), and **Pts to Proficiency** (`proficiencyGap` = `levelMin(subject,grade,3) − PM3`). Surfaced on Academic Trajectories drill-down + Academics band drawer (shared generic `BandStudentsDrawer` via `INSIGHTS_METRIC_COLUMNS`), Teacher Roster (attendance column only — FAST gaps already there), and Early Warning (Days abs. + worst-subject → L3). **Invariant: all three metrics flow from the shared helpers (`loadAttendanceMetrics` + `bucketFor`/`proficiencyGap`) on every surface so the same student shows identical numbers across Trajectory / Academics / Roster / Early Warning.** The drill-down also renders the full **PM progression** (Prior PM3 · PM1 · PM2 · PM3) via the shared exported `INSIGHTS_PM_COLUMNS` (prepended to `INSIGHTS_METRIC_COLUMNS` so both surfaces stay identical); PM cells render as roster-style **FAST achievement-level pills** (level color + click-to-flip to scale score) driven by a surface-wide **"Show: Level | Scale score"** toggle (`showScoreToggle`), with a numeric scale-score delta under each PM pill (PM1 vs Prior, PM2 vs PM1, PM3 vs PM2 — green growth / red decline) for full Teacher Roster parity via the shared `PmDelta`. Pill palette + context live in the shared `FastScorePill.tsx` (also imported by the Teacher Roster — single source). Server placements come from the shared `placePmSet()` (`fastCutScores.ts`), carried on the `levels` field of the band + trajectory endpoints; trajectory banding derives from the same call. **Invariant: the level palette and per-PM placement conventions are single-sourced (`FastScorePill` palette + `placePmSet`) so the drill-down pills match the Teacher Roster pills exactly.** Server endpoints (trajectory-students + academics band) pass `priorYearScore` + `pm2` (additive, school-scoped); trajectory CSV gained `prior_pm3` + `pm2`. **Full Teacher Roster parity**: both drill-downs also surface an **ELL** chip, a red **SP** active-safety-plan indicator, and the FAST **learning-gain green-check** (appended to the PM3 pill), and each row is clickable → opens the Student Profile (reuses `onOpenProfile`; score cells `stopPropagation` to preserve pill flip). Server decorates both endpoints' visible slice via shared `loadDrilldownContext` (ELL + safety plan) and `computeRowLearningGain`. **Invariant: the drill-down learning-gain check must agree with the Teacher Roster green-check — both flow from `loadFastHistory` historical PM3 + `decideLearningGain`, never `priorYearScore`.**
- **Data Chat Campaigns**: Admin-pushed, template-based teacher↔student check-in campaigns (Family nav, `activeSection: dataChats`, Core Team). Templates (built-in non-deletable "FAST Data Chat", lazily seeded per school; name/kind locked, checklist/chips/share editable; DELETE=archive) vs Campaigns — checklist/goal-chips/`shareWithFamilies` are **snapshotted at launch**. FAST campaigns assign students to ELA/Math teacher-of-record (subject ela|math|both) **or, with `assignment:"selected"` at launch, to hand-picked teachers of ANY department + responsible period (workload equity)** — kind stays `fast_data` (queue FAST pills gate on kind, not assignmentMode; `subject=null`, df scope falls back to failing-any); custom → selected teachers + responsible period. **Student scope selector** at launch: All / support flags (ese·is504·ell) / D-or-F in class (current grade <70 via `loadStudentGrades`; FAST pairs need the failing course to infer to the pair's subject) / hand-picked (max 500), with a preview-count endpoint; narrower scopes are **snapshotted into `data_chat_campaign_students`** and applied via `resolvePairs` on every downstream surface. Teacher flow: animated top-bar reminder icon + deadline banner (≤7 days) → queue modal with checklist, goal chips, FAST mini pills, past goals, and a **private teacher note that never reaches parent payloads**. **Inline roster chat**: Teacher Roster rows show a 💬 Chat button (tinted when the student is pending via `/data-chats/pending-students`) → `SelfDataChatModal`; pending-campaign students log toward the campaign, others log to a lazily created hidden per-school `self_serve` campaign (`/self-log`, `entry_seq` allows repeats; self-serve is `ne()`-excluded from queue/reminder/pending/lists). Parents see topics+goal on HeartBEAT (dashboard + PDF) only when the campaign shares. History: compliance table, topic coverage %, CSV export. **Invariant: the teacher log POST re-derives queue membership server-side (`resolvePairs`) — a teacher can only log their own assigned students; self-context/self-log require `teacherHasStudent` (own roster only); `loadStaff` 403s a non-SuperUser actor whose school ≠ `req.schoolId`.** See `routes/dataChats.ts`, `lib/db/src/schema/dataChats.ts`.
- **Teacher Roster**: Per-teacher student view with FAST scores, ESE/504/ELL flags, safety-plan indicators, and the FAST learning-gain green-check. Core Team can view any teacher's roster.
- **Student Profile (Student Lookup)**: Quick Access sidebar search → read-only "one-stop-shop" Student Snapshot (reuses the whole-child Student Profile with every edit affordance disabled, under the Teacher Roster `PrivacyGate`). Labeled **"Student Profile"** in the sidebar nav item, quick tile, and page heading, but the internal `activeSection` key stays `studentLookup` (a separate insights `studentProfile` deep page exists — do not conflate). The shared `StudentProfile.tsx` renders one top-to-bottom flex column ordered via CSS `order`: pinned header → Student schedule (collapsed) → Family → Spider/radar → Attendance & Flow → Behavior → Academics → Supports → Intervention history → Things-to-know → FAST Analysis (bottom). The collapsed **Student schedule** block ("View schedule") is backed by visibility-scoped `GET /api/student-lookup/:studentId/schedule` (`section_roster ⨝ class_sections ⨝ staff`, school-scoped, non-planning, ordered by period); it lazy-loads on first open and is `key={studentId}` so switching students remounts it (no stale cross-student rows). Car-rider/dismissal-mode editing is gated by `canManageDismissal` (admin + Core Team + counselor/guidance + front-office `cap_manage_dismissal`; mirrors `canManagePickup` minus confidential secretary) threaded App → StudentLookupPage → SnapshotView → StudentProfile, read-only otherwise. **Invariant: the client `canManageDismissal` prop and the server `canManageDismissal()` gate (used by both the dismissal-mode PATCH and the Student Profile chip) must admit the same roles.** The editable per-student parent-facing **"Message for this week's HeartBEAT"** note (collapsed editor, closed by default) surfaces on the Friday HeartBEAT (PDF block + email block, skipped when empty). Stored on `students` (`heartbeat_note` + updated_by/at). All lookup routes (`/api/student-lookup/search`, GET + PUT `/api/student-lookup/:id/heartbeat-note`, GET `/api/student-lookup/:id/schedule`) are visibility-scoped via the exported `getVisibleStudentIds` (teachers→own roster + trusted-adult; core team/admin/counselor→school-wide), matching the Student Profile endpoint's allowed set. **Invariant: the note read must NOT fall back to the school-only-scoped `GET /students/:id` — use the visibility-scoped lookup route or out-of-roster notes leak.**
- **Data Importer**: Generic CSV importer for assessments/rosters/behavior with template mapping, preview, commit, and rollback. **Delegable importers**: four assignable staff caps (`staff.capImportGrades/Attendance/Fast/Iready`, default FALSE) let Admin OR Core Team hand off a single importer to a non-admin clerk without granting admin. Assigned via a `StaffRolesMatrix` **import-only mode** (Core Team without full role authority sees ONLY the 4 import toggles; roles hidden, all other caps locked). A non-admin holder gets a dedicated **Data Imports** page (`activeSection: importData`, schoolAdmin nav group) rendering a `DataImports` wizard filtered to `allowedKinds` (gradebook→Grades, fast_*→Fast, assessments→iReady) plus the reused Eligibility `UploadTab` for Attendance. Per-kind enforcement is SERVER-side: `scope.ts` `SCHOOL_IMPORT_KIND_CAP`/`canImportKind`/`allowedSchoolImportKinds` gate every `dataImports.ts` surface (preview/commit/jobs/export/rollback/templates), attendance is gated by `requireAttendanceImporter` in `eligibility.ts` (upload + uploads history). `adminStaff.ts` lets Core Team into GET/PATCH staff but strips PATCH to ONLY the 4 import caps for non-full-authority actors (no privilege escalation). School-scoped only — district imports stay admin/SuperUser. **Invariant: importer authorization is enforced at each server route via the shared `canImportKind`/`requireAttendanceImporter`; client filtering (allowedKinds, import-only matrix) is UX only and bypassable. The PATCH field-strip is what prevents a Core Team actor from editing anything beyond the 4 import caps.**
- **Gradebook / Current Grades**: xlsx importer (`gradebook` kind in `dataImports.ts`) of one row per student×course (Other ID → `students.local_sis_id` in the active school; unmatched skipped + listed). Upload params: Quarter (Q1-Q4) + effective date. Stored in `student_course_grades`; **current grade per class** = selected quarter, fallback to latest populated quarter. Surfaced READ-ONLY on Student Profile (teacher, no download), Student Snapshot (Core Team, browser print), and parent comms (parent portal Academics tab + HeartBEAT PDF — reuses the academics `showFastScores` visibility gate, no new prefs columns). **GPA** (unweighted 4.0: 90-100=4/80-89=3/70-79=2/60-69=1/<60=0) is school-toggleable (`schoolSettings.gpaEnabled`, Core Team, **default OFF**; `gpa` is null when off) and averages only the **current semester's** courses (Fall=Q1/Q2, Spring=Q3/Q4 from the effective quarter). **Restorable rollback via job-chaining**: commit keeps the new + prior latest committed gradebook job (prunes older); reads come ONLY from the latest committed job; rollback restores the prior generation. **Invariant: gradebook rollback is single-step newest-first — the rollback route 409s any non-latest gradebook job, or the chain points at pruned (empty) rows. Current grade (whole-year fallback) and GPA (current-semester only) are separate computations in the shared `loadCurrentGrades`/`loadStudentGrades`.**
- **Intensive Group Insights**: Read-only Class Composer (proposes intensive-group sections from FAST item-level weakness) plus a Group Insights tab on Teacher Roster. Never writes to `section_roster`/`class_sections` — Skyward/RosterOne stays the source of truth.
- **School Tours (Enrollment Leads)**: Public bilingual (EN/ES) per-school brag page (`/tour/:schoolId`) with a tour-request lead pipeline (owners, timeline, overdue clock, conversion report), brag-sheet/post-tour PDFs, flyer/photo uploads, district branding, and admin-configured tour checkpoints. **Live Tour Capture (Phase 4)**: a QR on the roadmap (printed PDF + on-screen lead view) opens a token-gated, guide-facing, offline-first live-walk screen (`/tour/walk/:token`) — guide confirms/changes who is guiding (default = lead owner), taps once per checkpoint (client-timestamped, localStorage-buffered, synced when online), and jots optional staff-only per-stop notes. Captured timings feed the lead drawer (per-stop planned-vs-actual in chronological completion order, total length, follow-up notes) and outcomes metrics (avg tour length + per-guide rollup). **Invariant: walk step taps validate against the lead's eligible stops (family selections + always-included), not the whole school checkpoint catalog.**
- **Parent Pick-Up Module**: Curb keypad (`/pickup/curb`), walker gate (`/pickup/walkers`), admin tag authorization + QR tag PDFs, classroom signage tile, and an Admin Hub "still on campus" reconciliation tile. Role gate `canManagePickup`. **Front-office manual override**: pickup authorizations sync from RosterOne, but the office can override car-tag/rider/auth details by hand — reason-required (>=5 chars), audited (`pickup_override_audit`), and **temporary (expiry) or permanent**. Office override WINS until manually cleared (= Deactivate-with-reason; bulk-assign skips `isSyncProtected` rows so a re-import never clobbers them) and is **loudly flagged when RosterOne disagrees** via the override reconciliation tile (`/pickup/overrides/reconciliation`). Dismissal-mode changes capture a reason too. **Invariant: the "RosterOne disagrees" flag is `!sisMayHaveContact` — the client badge and the server `sisDisagrees` count must use the same direction.**
- **Event Ticketing (Phase 1)**: Free-ticket events with per-grade quotas, QR tickets emailed per student (inline + PDF + Parent Portal delivery), in-app and no-login volunteer scanning (atomic first-scan-wins), capacity caps, and a scan-history audit. Role gate `canManageTickets`.
- **Student Metrics Engine + Snapshot/Summary**: Shared per-student whole-child metrics engine (`lib/studentMetrics.ts` `loadStudentMetrics` + `computeCohortComparison`) — date-range aware (default YTD=Aug-1), school-scoped, batchable: attendance %/days absent, tardies, hall passes, lost instruction, behavior pullouts, OSS/ISS **served** days (distinct served day-rows), PBIS net, active MTSS tier flags, FAST PM by subject. Powers (A) a **Student Summary** bulk dataset in Data Export (`exportRegistry.ts` key `student_summary`) and (B) a visual **Student Snapshot** report page (`StudentSnapshotPage.tsx`, Core Team only) comparing one student to their **grade cohort** (radar oriented higher=healthier vs cohort-median ring, peer bars w/ percentile, distribution strip, supports + FAST trajectory) via `GET /api/exports/snapshot/:studentId`. **Invariant: both surfaces flow from the shared engine; cohort comparison suppresses below min cohort n=10 (no mean/percentile/distribution leak); FLEID boundary — `localSisId` only, never `studentId`; snapshot gated isCoreTeam + `getVisibleStudentIds` with school+grade-scoped cohort.** The Snapshot's **"FAST Progress Monitoring"** section and the **Family HeartBEAT PDF** render Teacher-Roster-parity FAST (achievement-level pills/sub-levels, points-to-next-level, points-to-proficiency, learning-gain check) — single-sourced via `lib/fastParity.ts` `loadStudentFastParity` (`placePmSet`/`bucketFor`/`proficiencyGap`/`computeRowLearningGain` in `lib/learningGains.ts`, "current year" from DATA via `resolveCurrentFastYear`). Snapshot = full interactive parity (level↔score flip toggle, momentum markers); HeartBEAT PDF = STATIC colored sub-levels + pts-to-next below PM3 + LG (PDF-only, gated by `showFastScores`, `localSisId` only). **Invariant: Snapshot + HeartBEAT FAST numbers flow from `loadStudentFastParity` so they match the Teacher Roster / Insights drill-downs exactly; the PDF LG check is a vector stroke (WinAnsi can't render ✓).**
- **School Grade Estimated Calculator (Phase 1)**: Admin/Core-Team estimate of the Florida MS school grade per PM window (9 components ×100). FAST components auto-computed; LG is a projection at PM1/PM2 and strict PM3-to-PM3 at PM3. Role gate `canManageSchoolGrade`. **Invariant: LG prior-year evidence comes from `loadFastHistory` historical PM3 (same source as the Teacher Roster green-check), never `priorYearScore` — the two surfaces must agree.**
- **Historical FAST (multi-year)**: Admin/Core-Team "Historical FAST" table on the **Student Profile** (Student Lookup / Snapshot only — NOT teacher roster, parent portal, or HeartBEAT). Shows the **current year as the top anchor row** then comparable prior years (FAST launched 22-23; older FSA is a non-comparable scale so it's excluded), with **within-year growth** (PM1→latest same-year PM) and a per-year learning-gain flag; **no cross-grade summed total** (grade bands differ year to year). Full PM1/PM2/PM3 render as FAST level pills. Server: `lib/fastHistory.ts` `loadFastFullHistory`; route in `routes/studentLookup.ts` (visibility-scoped via `getVisibleStudentIds`, `localSisId` only — **never FLEID**). Assignable via cap **`capViewFastHistory`** which **only Admin/SuperUser may grant** — Core Team (incl. `capStaffRoles` holders) CANNOT delegate it (enforced by an explicit admin-only gate in `adminStaff.ts` PATCH, since `capStaffRoles` otherwise passes `hasFullRoleAuthority`). Dev demo data seeded for the demo school by `seedHistoricalFastIfEmpty`. **Invariant: "current year" for both the seed and the read route is resolved from DATA (`resolveCurrentFastYear` = MAX `school_year` WHERE `is_historical=false`), NOT the wall clock — a frozen demo dataset drifts past the July school-year boundary, and a wall-clock "current" would drop the real anchor row and shift grade-in-year math by a year.**

## User preferences

_Populate as you build_

## Onboarding (critical setup notes)

- **Configure a default bell schedule before enabling Hall Pass Queue.** The
  queue auto-resets when the bell-schedule period changes, giving each
  period a clean line. If no default bell schedule is configured, the
  queue falls back to 45-minute idle buckets — usable, but the period-aware
  reset is the intended UX. School Settings → Bell Schedules → mark one
  schedule as the default.

## Future work

### Recently shipped

Archived to `docs/shipped.md` (most-recent first; reference only, no
remaining action). When you ship something new, add a bullet to the top
of `docs/shipped.md`.

### Open work

Full detail for each item lives in `docs/future-work.md`. Summaries:

- **Family Messages — multi-contact email (Phase 2).** True multi-contact email; preserve "deliver to many, attribute to one." Blocked on SIS adapter not feeding guardian emails.
- **E-sign signing campaigns (bulk send → per-student return).** Wrap per-doc signing in a campaign with one copy per `student_id`; field-trip-list dashboard.
- **LG subject-band promotions (Algebra I etc.).** Within-level LG credit SHIPPED; subject-band promotions still uncredited until the importer captures prior course code.
- **Historical FAST data + Algebra I placement review.** Prior-year FAST import toggle + placement-review report/override; later longitudinal multi-year dashboards.
- **AI Consistency Check — onboarding step + admin telemetry tile.** Guardrails onboarding step + admin "this month" telemetry tile.
- **School-local timezone — per-school IANA column.** Replace `DEFAULT_SCHOOL_TZ` constant with a per-school column before onboarding a non-Eastern school.
- **Refresh Core Team "How this works" copy after Phase 4 case enhancements ship.** One help-copy pass after Phase 4.
- **Pickup module — small follow-ups.** 5-digit number expansion path; optional in-line chime.
- **Student Photos.** Object-storage-backed student photos (bulk ZIP + per-student capture) with consent flag; prerequisite for walker verification.
- **Witness statement numbering — UI surfacing.** Surface the formatted ID across drawers/PDF/audit log; backfill existing.
- **AST follow-ups.** Transfer zero-out hook, optional Friday digest email, per-staff ledger drilldown.
- **Feature licensing Phase 4 candidates.** Wire a third quota consumer; per-feature usage charts.

## Gotchas

- **Timezone handling**: Be careful with date comparisons and `new Date()` as it can lead to UTC pitfalls. Use local `YYYY-MM-DD` strings for comparisons.
- **Multi-tenancy on unique indexes**: `student_id` and `displayName` are not globally unique. Ensure every query touching tenant-scoped tables includes `eq(table.schoolId, schoolId)`. Unique indexes are typically composite `(school_id, column)`.
- **API route shadowing**: When adding new API routes, especially with dynamic segments, ensure more specific routes are defined before broader ones to prevent shadowing.
- **Cron jobs and environment variables**: Cron jobs are often conditional on `NODE_ENV` and other environment flags. Verify `EMAIL_REMINDERS_ENABLED` and `RESEND_FROM_ADDRESS` for email functionality.
- **Drizzle-kit `db push`**: It can be blocked by interactive prompts on rename detection. For additive schema changes, direct `ALTER TABLE` SQL is often used as a workaround. Ensure `lib/db/src/schema/*.ts` files are updated to reflect the true schema.
- **NO FLEID forward-facing — EVER (use `local_sis_id`).** The canonical `students.student_id` (the FLEID, e.g. `FL000000539119`) is an INTERNAL identifier — a foreign key only. It must NEVER be rendered as visible text to any user (staff, parent, student) anywhere: table cells, labels, badges, tooltips, headings, search results, @mention tokens, graph nodes, kiosk/signage, AND CSV/PDF exports. The ONLY display ID is `students.local_sis_id` (the local SIS ID). When a surface needs a student ID label, the server response must carry `localSisId` and the UI renders `localSisId ?? "—"` — never fall back to `studentId`. Keep using `student_id` for FKs, API path params, React keys, and lookups (it is the join key; remember it is NOT globally unique — always pair with `school_id`). When adding ANY student-facing surface or export, audit it for raw `studentId` rendering before shipping. See `lib/fleid` boundary precedent in `routes/kiosk.ts` (kiosk/badges) and `routes/safetyPlans.ts` (`/safety-plans/list` returns `localSisId`).
- **Public-facing URLs / QR codes (`publicAppOrigin`)**: Links families open OUTSIDE the workspace (post-tour survey QR, brag-page link, lead-notify email) must NOT be built from `$REPLIT_DEV_DOMAIN` — it is the *development* host and is often unset in production, so QR codes ended up pointing at the dev URL or `http://localhost:5000` (a dead page). Resolve origin as: `PUBLIC_APP_URL` → first `$REPLIT_DOMAINS` host (published prod domain in prod, dev/preview host in dev) → inbound request forwarded host → localhost. See `publicAppOrigin(req)` in `routes/tours.ts` and `kioskBaseUrl` in `routes/kiosk.ts`.
- **PDFs / blobs in the preview iframe**: The session cookie is blocked inside the Replit preview iframe (the app falls back to a Bearer token in sessionStorage, `pulseed.authToken`). A blob URL opened in a new tab renders blank, and `window.open(...).print()` can deadlock and freeze the app. For authed PDFs, trigger a blob **download** (`a.download`) from the current document instead of opening/printing in a tab — this is why the Tours lead-drawer PDFs download to disk.

## Pointers

- [pnpm-workspace skill](https://replit.com/path/to/pnpm-workspace-skill-docs): For workspace structure, TypeScript setup, and package details.
- [Drizzle ORM documentation](https://orm.drizzle.team/docs/overview): For database schema and query building.
- [Zod documentation](https://zod.dev/): For data validation.
- [Orval documentation](https://orval.dev/): For OpenAPI-based API code generation.
- [Recharts documentation](https://recharts.org/en-US/guide/GettingStarted): For charting components used in dashboards.
- [pdfkit documentation](http://pdfkit.org/docs/getting_started.html): For server-side PDF generation.

## Spotlight governor (v2)

Quartile-tiered point pool that activates only when the house
standings gap exceeds `RUNAWAY_LEADER_THRESHOLD` (1500). Pools by
rank when active: top `{1,2,3}` / upper-mid `{2,4,6}` / lower-mid
`{4,6,8}` / bottom `{6,8,10}`; healthy race draws `{1..10}`.

Key invariant: **the value the teacher sees IS the value the DB
stores** — `/spotlight/pick` bakes the pool-correct value into the
reveal; `/spotlight/award` re-validates and returns 409 ("re-spin")
if standings shifted. Strict integer 1..10 validation; no coercion.

Helpers + per-house rotation in `routes/spotlight.ts`
(`isRebalancerActive`, `poolForHouse`, `pickFromPool`,
`computeHouseTotalsForCap`).
