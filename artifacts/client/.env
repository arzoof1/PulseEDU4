# Frontend — production site: https://pulseedu.pulsekinetics.us/
# Built app uses same-origin /api. API_PROXY_TARGET is only for `pnpm run dev`.
PORT=5173
BASE_PATH=/
API_PROXY_TARGET=https://pulseedu.pulsekinetics.us

# Local dev (uncomment when running Vite against a local API on :8080):
# API_PROXY_TARGET=http://127.0.0.1:8080