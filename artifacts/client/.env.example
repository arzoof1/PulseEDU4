# Copy to `.env` for local development.
# Production build for https://pulseedu.pulsekinetics.us/ uses BASE_PATH=/ and same-origin /api.

PORT=5173
BASE_PATH=/

# `pnpm run dev` uses `.env.development` (committed) → http://127.0.0.1:8080
# Override here only if your API runs on a different host/port.
